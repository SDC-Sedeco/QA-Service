/**
 * Mimics user behavior (i.e report questions, mark as helpful, post a question and answer, get answers and questions lists)
 * at least one test per service
 * Puppeeter, nightwatch, cypress
 */

 describe('Sample', () => {
  it('should test that true === true', () => {
    expect(true).toBe(true)
  })
})